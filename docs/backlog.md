# 오토로그 백로그

1차(MVP)가 끝난 뒤 남은 작업 목록. 1차 범위는 `docs/tasks.md`, 제품 기획은 `docs/planning.html`을 따른다.

- **A. 1차에서 발견한 숙제** — 구현·검증 중 드러났고 1차 범위 밖으로 미룬 것
- **B. 기획된 다음 단계** — `planning.html` 5장의 2차·3차·n차

## A. 1차에서 발견한 숙제

| # | 우선순위 | 작업 | 남긴 이유 | 완료 기준 |
|---|---|---|---|---|
| B0 | 높음 | 순서 변경 이동량 오차 (버그) | 손잡이를 한 행(216px)만 내려도 10칸 아래로 떨어진다. `clip_order` 저장값으로 확인 — 입력 이벤트 수와 무관하다. `list/DragDropState.kt:69` `onDrag` 의 목표 위치 계산이 레이아웃 갱신 전 값을 보는 것으로 의심 (2026-09-19 SM-F971N) | 끈 거리만큼만 이동, 계측 테스트로 고정 |
| B1 | 높음 | 병합 전경 서비스 | 지금은 일반 백그라운드 작업이라 아주 긴 병합은 시스템이 뒤로 미룰 수 있다 (#26) | `setForeground`로 승격, 앱 이탈 상태에서 장시간 병합 완주 |
| B2 | 중간 | 소유권이 풀린 옛 브이로그 정리 | 앱을 지우거나 앱 데이터를 비우면 MediaStore가 소유자 표시를 떼어, 그 뒤로는 옛 결과물을 말없이 지울 수 없다. 같은 날짜 파일이 갤러리에 하나 더 남는다 (#34) | `MediaStore.createDeleteRequest`로 사용자 확인을 받아 중복 정리 |

## B. 기획된 다음 단계

| 단계 | 작업 | 비고 |
|---|---|---|
| 2차 | 편집 화면 진입 경로 (미리보기 → 편집) | 이 경로가 2차에서 처음 열린다 |
| 2차 | 클립 구간 지정 + 자막 렌더링 | 클립 단위 자막 추가 |
| 3차 | 구간 자르기(트리밍) | |
| 3차 | 앞/뒤 3초 이어보기 | 편집 결과 확인용 |
| 3차 | 편집 여부 배지 실제 연결 | 1차에서 필드만 넣어 둠 (#18) |
| 3차 | 촬영 후 즉시 편집 | 편집 기능에 종속돼 1차 제외 |
| n차 | 해상도·프레임레이트 옵션 | 1차는 1080×1920·30fps 고정 |
| n차 | 가로(16:9) 촬영 지원 | 1차는 세로 고정 — 병합 시 회전 보정·레터박스 처리가 함께 필요해진다 |
| n차 | 배경음악 삽입 | 1차는 원본 오디오 유지 |
